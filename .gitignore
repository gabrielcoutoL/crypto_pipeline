# 1. Ambientes Virtuais e Dependências (O Git não precisa subir o que o uv pode instalar de novo)
.venv/
__pycache__/
*.pyc
.vscode

# 2. Arquivos de Ambiente / Segredos (NUNCA SUBIR ISSO)
.env
.env.*
secrets/

# 3. Dados e Logs (NÃO suba a carga, suba apenas o código que gera a carga)
data/
*.parquet
*.csv
*.json
*.log

# 4. Arquivos do Sistema Operacional (Lixo do Windows/Mac)
.DS_Store
Thumbs.db